# Mission: CH9141K 双车通信

## Why
为 2022 年 TI 杯 C 题建立可靠的领头车到跟随车无线控制链路，使跟随车上电等待、由领头车统一启动和停止，并在失联时安全停车。

## Success looks like
- 能独立把领头车 CH9141K 配成主机、跟随车 CH9141K 配成从机
- 能用从机 MAC 配置掉电保存的自动重连
- 能通过 `C:t:r\r\n` 同步帧和 500 ms 失联制动验证整条链路
- 能根据 AT 返回值快速判断角色、配对或串口接线问题

## Constraints
- 两车 MCU 均为 MSPM0G3507，CH9141K 串口为 115200-8N1
- 保留现有工程的 UART2 引脚分配，不在本课中修改固件
- 遵守 C 题规则：跟随车只有上电开关，行驶时不得依赖车外通信

## Out of scope
- CH9141K 广播模式和一对多轮询
- 跟随距离控制、循迹 PID 和超车策略
